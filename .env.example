# Local development variables (no real secrets here)
POSTGRES_DB=message_db
POSTGRES_USER=message_user
POSTGRES_PASSWORD=change_me_local_password

DB_HOST=postgres
DB_PORT=5432
DB_USER=message_user
DB_PASSWORD=change_me_local_password
DB_NAME=message_db

MESSAGE_TTL_SECONDS=3600
CLEANUP_INTERVAL_SECONDS=30
WORKER_PORT=3002

FRONT_PORT=8080
API_PORT=3000

# Partie 5 pulse (Phase 2)
TABLEAU_URL=
GROUPE=team-demo
COULEUR=blue
VERSION=dev

# Production image references (set by CI/CD or deployment environment)
FRONT_IMAGE=your-user/bataille-navire-front:latest
API_IMAGE=your-user/bataille-navire-api:latest
WORKER_IMAGE=your-user/bataille-navire-worker:latest
