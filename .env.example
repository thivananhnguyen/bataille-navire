# Local development variables (no real secrets here)
POSTGRES_DB=message_db
POSTGRES_USER=message_user
POSTGRES_PASSWORD=change_me_local_password

DB_HOST=postgres
DB_PORT=5432
DB_USER=message_user
DB_PASSWORD=change_me_local_password
DB_NAME=message_db

MESSAGE_TTL_SECONDS=3600
CLEANUP_INTERVAL_SECONDS=30
WORKER_PORT=3002

FRONT_PORT=8080
API_PORT=3000
PROMETHEUS_PORT=9090
GRAFANA_PORT=3001
GRAFANA_ADMIN_USER=change_me_grafana_admin
GRAFANA_ADMIN_PASSWORD=change_me_grafana_password

# Partie 5 pulse (Phase 2)
TABLEAU_URL=https://services-battle.vercel.app
GROUPE_DISPLAY=Bataille des Navires
GROUPE_SLUG=bataille-des-navires
COULEUR=#0071BC
VERSION=dev
TAG=dev
REGISTRY=your-dockerhub-user
PAVILLON_FICHIER=/data/pavillon.txt

# Production image naming (compose.prod builds full image names from REGISTRY/GROUPE_SLUG/TAG)