apiVersion: v1
kind: Secret
metadata:
  name: flotte-secrets
  namespace: flotte
type: Opaque
# Keep this manifest value-free in git.
# Create real values at deploy time, for example:
# kubectl -n flotte create secret generic flotte-secrets \
#   --from-literal=DB_PASSWORD='...' \
#   --from-literal=POSTGRES_PASSWORD='...' \
#   --dry-run=client -o yaml | kubectl apply -f -
stringData:
  DB_PASSWORD: ""
  POSTGRES_PASSWORD: ""
